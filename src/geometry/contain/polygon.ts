

interface Point {
  x: number;
  y: number;
}

export function pointInPolygonFill(points: Point[], x: number, y: number): boolean {
  return false;
}

export function pointInPolygonStroke(points: Point[], closed: boolean, lineWidth: number, x: number, y: number): boolean {
  return false;
}